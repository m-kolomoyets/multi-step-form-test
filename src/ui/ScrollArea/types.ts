import {
    Root as ScrollAreaPrimitiveRoot,
    ScrollAreaScrollbar as ScrollAreaPrimitiveScrollAreaScrollbar,
} from '@radix-ui/react-scroll-area';

export type ScrollAreaProps = React.ComponentProps<typeof ScrollAreaPrimitiveRoot>;
export type ScrollAreaScrollBarProps = React.ComponentProps<typeof ScrollAreaPrimitiveScrollAreaScrollbar>;
